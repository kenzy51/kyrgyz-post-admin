import React from 'react';
import CreateMessageForm from 'src/widgets/CreateMessagePage/CreateMessageForm/CreateMessage';

const CreateMessagePage = () => {
    return (
        <div>
            <CreateMessageForm/>
        </div>
    );
};

export default CreateMessagePage;